Task 1: Genre-Based Recommendation System
- Update the book database schema to include a robust genre/category field such as Romance, Sci-Fi, or Motivational.
- Implement book title search functionality.
- Add recommendation logic that detects the searched book's genre and returns other books in the same genre.
- Update the search results UI to show the primary searched book first, followed by a "Recommended for you" or "Similar Books" section with genre-matched results.

Task 2: User Reading History
- Implement backend tracking to store books a specific user interacts with or completes, such as in a user_read_history table.
- Create an API endpoint that returns a logged-in user's previously read books.
- Build a homepage component titled "Books you've read before."
- Place the history component prominently on the landing page or dashboard, either at the top or in the middle depending on the final UI mockup.

Task 3: Admin Dashboard
- Set up role-based access control so only authorized admin users can access dashboard routes.
- Scaffold the main admin panel layout and navigation.
- Implement CRUD functionality for managing the book catalog.
- Implement functionality for managing user accounts and viewing system analytics.
